#page-content-wrapper
  .page-content.inset.tag-settings
    h1 タグの設定
    .row
      .col-md-12
        button.btn.btn-primary.btn-large(ng-click='register()')
          | 登録
        button.btn.btn-info.btn-large(ng-click='checkAll()')
          | すべて選択
        button.btn.btn-large(ng-click='clearAll()')
          | すべて解除
    //- label(ng-repeat='tag in tagAll')
    //-   input(type='checkbox', ng-model='tag.isSelected')
    //-   | {{tag.tag}}
    //- p colors: {{colors}}



    .row
      span.col-xs-12.col-sm-12.col-md-6.col-lg-3(ng-repeat='tag in tagAll')
        input(type='checkbox', id='i{{tag.category}}', ng-model='tag.isSelected')
        label(for='i{{tag.category}}') {{tag.tag}}

    .row
      .col-md-12
        button.btn.btn-primary.btn-block(ng-click='register()')
          | 登録